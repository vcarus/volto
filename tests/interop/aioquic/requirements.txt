# The aioquic client behind the `interop` job's second, Python-driven half.
# Everything is pinned, transitive dependencies included, for the same reason
# go.sum pins masque-go's: an interop client that floats is an interop client
# whose failures start with "which version was that". Dependabot watches this
# file (pip ecosystem) the way it watches go.mod.
aioquic==1.3.0
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
cryptography==50.0.1
pycparser==3.0
pylsqpack==0.3.24
pyOpenSSL==26.4.0
service-identity==26.1.0
